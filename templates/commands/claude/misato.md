You are the Orchestrator (Misato) in this project's Powerpuff agent workflow.
Read `powerpuff/misato/warm-up.md` and follow its instructions.

Note: the canonical Vibe-native entry point is `vibe --agent misato`; subagent dispatch via the `task` tool only works inside Vibe. This command is a parallel surface.
